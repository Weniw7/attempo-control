import { integer, real, sqliteTable, text } from "drizzle-orm/sqlite-core";
export const suppliers = sqliteTable("suppliers", { id: integer("id").primaryKey({ autoIncrement: true }), name: text("name").notNull(), country: text("country"), website: text("website"), contactName: text("contact_name"), unitPrice: real("unit_price"), currency: text("currency").default("EUR"), moq: integer("moq"), gsmMin: integer("gsm_min"), gsmMax: integer("gsm_max"), leadTimeDays: integer("lead_time_days"), sampleCost: real("sample_cost"), qualityScore: integer("quality_score"), riskScore: integer("risk_score"), status: text("status").notNull().default("lead"), notes: text("notes"), createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull() });
export const conversations = sqliteTable("conversations", { id: integer("id").primaryKey({ autoIncrement: true }), externalThreadId: text("external_thread_id").unique(), channel: text("channel").notNull().default("instagram"), contactName: text("contact_name").notNull(), contactType: text("contact_type").notNull(), supplierId: integer("supplier_id").references(() => suppliers.id), lastMessage: text("last_message"), summary: text("summary"), suggestedReply: text("suggested_reply"), approvalStatus: text("approval_status").notNull().default("pending"), priority: text("priority").default("medium"), updatedAt: text("updated_at").notNull() });
export const opportunities = sqliteTable("opportunities", { id: integer("id").primaryKey({ autoIncrement: true }), title: text("title").notNull(), type: text("type").notNull(), stage: text("stage").notNull().default("evaluation"), expectedRevenue: real("expected_revenue"), committedCost: real("committed_cost"), probability: integer("probability"), nextAction: text("next_action"), dueDate: text("due_date"), ownerEmail: text("owner_email"), createdAt: text("created_at").notNull(), updatedAt: text("updated_at").notNull() });
export const costScenarios = sqliteTable("cost_scenarios", { id: integer("id").primaryKey({ autoIncrement: true }), name: text("name").notNull(), supplierId: integer("supplier_id").references(() => suppliers.id), baseGarment: real("base_garment").notNull(), decoration: real("decoration").default(0), packaging: real("packaging").default(0), freightAndDuties: real("freight_and_duties").default(0), fees: real("fees").default(0), retailPrice: real("retail_price").notNull(), createdAt: text("created_at").notNull() });
